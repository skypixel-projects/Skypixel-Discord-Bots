#
#Aici sunt toate linile de coduri salvate pentru ori ce cauza!
#botul nu poate citi aces fisier!
#

var embed = new Discord.MessageEmbed()
    .setAuthor(lang_en.maintenance)
    .setColor('RED')
            
message.channel.send(embed);